---
title: "Docker platform notes"
description: "How the KloudMate agent runs in Docker mode: infrastructure and eBPF coverage across containers, and how application tracing works."
sidebar:
  order: 3
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

How the agent runs in Docker mode. For installation, see the [Docker installation guide](../../installation/docker-agent/).

## What works today, with no injection

Running in Docker mode, the agent already covers your containers without touching them:

- **Infrastructure, metrics, and logs:** host and container metrics and container logs, from the containerized collector. See [Host metrics and logs](../../baseline/host-metrics-and-logs/).
- **eBPF monitoring:** the extended Berkeley Packet Filter (eBPF) runs in the host kernel, which containers share. It sees container processes and traffic there without touching them. You get Rate, Errors, and Duration (RED) metrics, the service map, and Database Activity Monitoring across your containers with no code changes and no injection. See [eBPF observability](../../ebpf-observability/).

## Application-level tracing in containers

eBPF monitoring traces every container the same way. RED metrics, the service map, and Database Activity Monitoring are there from install, with no per-container work. Whether the agent adds full, span-level application traces on top depends on the runtime:

| Runtime | Span-level tracing in Docker | What you do |
|---|---|---|
| PHP 7 and 8 | Injected directly. The agent installs the tracer inside the running container and reloads the web server, with no redeploy. | Nothing, it is automatic. See [PHP container instrumentation](#instrument-php-containers). |
| Java, Node.js, Python, .NET | eBPF monitoring only. The agent does not inject a language agent into a running container. These runtimes read their instrumentation from the startup environment, which cannot change unless the container is recreated, and the agent does not recreate containers. | [Add OpenTelemetry to the application](#send-full-opentelemetry-traces-from-your-containers) and point it at the agent, or run the service on a [Linux host](../../installation/linux-agent/) or [Amazon ECS](../../installation/ecs-agent/), where the agent instruments it for you. |
| Go | eBPF monitoring only. Go compiles to a static binary with no runtime to attach to. | Nothing. See [Go and eBPF](../../auto-instrumentation/go-ebpf/). |

Whichever runtime you run, eBPF monitoring keeps covering the container, so you have RED metrics and a place on the service map before you add span-level tracing.

## Instrument PHP containers

In Docker mode, the agent instruments your PHP containers automatically and reloads the web server.

**Narrow or turn it off** with environment variables on the agent:

```bash
# Limit instrumentation to specific containers (default: every PHP container)
KM_CONTAINER_INSTRUMENT_NAMES=app1,app2
KM_CONTAINER_INSTRUMENT_IMAGES=wordpress,my-php-app

# Turn PHP container instrumentation off entirely
KM_CONTAINER_INSTRUMENT_ENABLED=false
```

### What is supported

- **Web servers:** Apache with mod_php, and PHP-FPM. The agent reloads them gracefully, so tracing starts without a restart. WordPress and the official `php:8.x-apache` images work this way.
- **PHP versions:** 7.0 through 8.x. The tracer is matched to the container's exact PHP build automatically.

The agent cannot instrument a container that serves requests from the **PHP built-in server** (`php -S`), because that server does not reload configuration without a full restart. The official Adminer image is one example. Alpine (musl) images are also a problem, because the bundled tracer assets target glibc.

### These containers are configured by environment, not the UI

PHP container instrumentation is set through the environment variables above, so instrumented containers do **not** appear in the agent's **Discovered Services** or APM settings screen. That list covers processes on a host and workloads on Kubernetes, not containers on a Docker host.

## Send full OpenTelemetry traces from your containers

For Java, Node.js, Python, and .NET, full traces come from OpenTelemetry running inside the application. Add it once, at build or deploy time, and point it at the agent. The agent already listens for OpenTelemetry (OTLP) data:

- gRPC on port `4317`
- HTTP on port `4318`

**1. Instrument the application.** Add OpenTelemetry to the service, with either the language's zero-code auto-instrumentation (the Java agent jar, the Node.js `--require` loader, and so on) or the OpenTelemetry SDK. These are the same language agents the agent adds automatically on a host, listed in the [application APM overview](../../auto-instrumentation/overview/).

**2. Point it at the agent.** Set these environment variables on the application container:

```bash
OTEL_EXPORTER_OTLP_ENDPOINT=http://AGENT_HOST:4318
OTEL_SERVICE_NAME=your-service-name
```

Replace `AGENT_HOST` with an address that reaches the agent from inside the container:

- **Docker Desktop (Mac or Windows):** `host.docker.internal`.
- **Linux:** the Docker bridge gateway, commonly `172.17.0.1`. Or add `--add-host=host.docker.internal:host-gateway` to the application container and use `host.docker.internal`.
- **Same Docker network:** if the agent runs as a service on the same user-defined network as the application, use its service name, for example `http://kmagent:4317`.

To send over HTTP instead of gRPC, use port `4318` and add `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`.

**3. Restart the service** so the instrumentation loads. Its traces then appear in KloudMate, joined to the service's eBPF spans on one trace.

The agent produces this same result automatically on a [Linux host](../../installation/linux-agent/) or on [Amazon ECS](../../installation/ecs-agent/). In plain Docker you add the instrumentation yourself, because the agent cannot change a running container's startup environment without recreating it.

## Deployment posture

PHP container instrumentation reaches the containers through the Docker socket, so it works from a host-process agent or from the containerized collector, as long as the socket is mounted.

eBPF monitoring is different: it needs host kernel access, so a plain, non-privileged agent container cannot produce the eBPF signals. Run the agent on the host, or as a privileged container with host mounts.

## PHP 7 in containers

A tracer built on the host must match the container's exact PHP build, so PHP 7 in a container is the hardest case. The reliable path is to add the tracer at image build time. See [PHP instrumentation](../../auto-instrumentation/php/).

## Next steps

<CardGrid>
  <LinkCard title="Install with Docker" href="../../installation/docker-agent/" description="Run the agent as a container." />
  <LinkCard title="eBPF observability" href="../../ebpf-observability/" description="The automatic monitoring that covers containers." />
  <LinkCard title="Application APM overview" href="../../auto-instrumentation/overview/" description="How opt-in tracing works." />
</CardGrid>
